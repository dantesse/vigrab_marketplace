#Lucia Auth
AUTH_SECRET=

#Uploadthing (v7)
#Dashboard -> API Keys -> UPLOADTHING_TOKEN. This replaces the v6 pair
#UPLOADTHING_SECRET / UPLOADTHING_APP_ID, which the app no longer reads.
UPLOADTHING_TOKEN=

#Postgres URI
DATABASE_URL="postgresql://rushikesh:password@localhost:5432/Bid"
WS_URL="ws://localhost:8080"